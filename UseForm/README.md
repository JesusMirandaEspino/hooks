# UseForm

Ejemplo:

```

    const initialForm = {
        name:   '',
        age:    0,
        email:  ''
    }

    const [  setValues, handleInpuntChange, reset ] = UseForm( initialForm  );

```